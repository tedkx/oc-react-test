{
  "name": "another-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "eurobank-react-scripts": "^1.1.4-17",
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "serve": "^10.0.1"
  },
  "scripts": {
    "serve": "serve -s build -l 8080",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
